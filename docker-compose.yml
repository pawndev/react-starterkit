version: '2'
services:
  front:
    build:
      context: ./front
      dockerfile: Dockerfile
    entrypoint: "npm run start"
    ports:
      - $FRONT_REACT_PORT:$REACT_PORT
    volumes:
      - ./front/src:/usr/src/app
